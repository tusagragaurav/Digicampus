# TODO: Fix Error in JitsiMeet.tsx

## Tasks
- [x] Remove unused `useEffect` import from React
- [x] Add `domain` prop to JitsiMeetProps interface
- [x] Add `domain` prop to JitsiMeeting component (default to 'meet.jit.si')
- [x] Fix `toggleLobby` command to `setLobbyModeEnabled` (already correct)
- [x] Run TypeScript check to verify no errors
